# Dependencies needed to run linting and testing locally or in CI.
# The mini_calc package itself has NO runtime dependencies (see
# requirements.txt) -- these are development/CI tooling only.
#
# Installed automatically by .github/workflows/ci.yml on every run.
# To install locally: pip install -r requirements-dev.txt

flake8>=7.0,<8.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
